<configuration>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/market.log</file>
        <append>true</append>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/archive/market.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>512MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%date %level [%thread] %logger{10}:%line - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%date %level [%thread] %logger{10}:%line - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.gmail.eksuzyan.pavel.education.market" level="DEBUG">
        <appender-ref ref="FILE"/>
    </logger>

    <root name="com.gmail.eksuzyan.pavel.education.market" level="WARN">
        <appender-ref ref="STDOUT"/>
    </root>

</configuration>